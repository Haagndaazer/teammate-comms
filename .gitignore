# Python / uv
.venv/
__pycache__/
*.pyc
dist/
*.egg-info/
.pytest_cache/

# Local dev tooling — NOT part of this plugin (teammate-comms ships mcpServers
# inline in plugin.json). The .mcp.json present here is a vibe-cognition dev
# config for working in this repo; do not commit it.
.mcp.json

# Keep uv.lock tracked (reproducible installs) — do not add it here.
