teammate-comms audit-fix backlog, managed by Silvie (planner/manager, no code), implemented by Svetlana. Source: AUDIT-v0.7.0.md finding IDs. Work flowed as work packages WP-1..WP-10, one branch per WP (fix/audit-wpN — unlike the later 260701 epic's single shared branch). Quality bar (every WP): power-user scrutiny, hermetic tests in test_handshake.py for the failure mode + boundaries, full harness green on Windows, pure stdlib zero deps, known-intentional table untouchable, docs updated in the same WP, Silvie code-reviews every diff before merge with sign-off naming the exact commit SHA (merge must be that SHA), fix+proof same commit with failure tests asserting the REASON, non-code outputs checked (journal protocol: flush to main via temp worktree at checkpoints; destructive git ops need a flush ping), mirror-pattern briefs must NAME the parts not to copy, constraint re-grep on moved surfaces. Pipeline outcomes: WP-1 missed-event correctness @1103be2; WP-2 doc-drift+dead code @72b92ec (micro-CR); WP-9 wake reliability re-nudge (new H-1 finding: Claude Code drops channel notifications, GH #38736/#61797) @6d86b46; WP-3 test/ops floor+CI @e9cd571; WP-4 dashboard send parity @9703874; WP-5 hardening/trust-model @23f8a2c; WP-6 race+scale part 1 (A-7 pid-owner lock steal) @5839750; WP-7 scale part 2 (four phases: inbox paging, seek-tail, deletions compaction, byte cursor) @a9f3af7; WP-8 F/G batch @227b2e1 (B-4 deferred); WP-10 authority-coordination rule (Colton-requested, v0.8.1). Working agreement: implementer works queue top-down without go-aheads, plan-as-FYI, diff is the hard gate, start next WP while awaiting review, branch-switch journal protocol (adopted from vince/Vorpid), release pushes via gh pr merge --match-head-commit. Retired: the graph became the backlog (task nodes), and this file moved to docs/history in the 260701 epic.