Codex Parity Plan — teammate-comms v0.16.0 (+ v0.17.0 root move). PLAN rev 3, APPROVED-WITH-CHANGES by sonnet verification 2026-09-08 after rev 1 (REJECT, 14 findings R1–R14) and rev 2 (REJECT, 9 findings F1–F9); four V-corrections applied.

Goal: a Codex user installs teammate-comms like a Claude Code user (marketplace + one restart), registers, and messages any teammate on the machine; Claude Code behaviour unchanged except the version and a new harness: line. Maintainability contract: adding harness #3 touches one harness.py row, one adapters/<harness>/ dir, one PARITY.md column, and only if needed one Waker class registered in channel.WAKERS and one spawn builder in spawn.SPAWN_BUILDERS; a per-file literal ratchet (committed baselines of codex/claude occurrences per src file, harness.py exempt) enforces it. Two releases: v0.16.0 Codex parity (WP-43..46 + Gate TC-C1); v0.17.0 comms-root move (WP-47).

Principles: policy in the server (harness.py table keyed by TEAMMATE_HARNESS, default claude-code, no clientInfo sniffing); adapters copied from vibe-cognition 5b58b07 with names swapped; no template renderer (SKILL harness-neutral body + Harness notes + lint); PARITY.md with completeness test; conformance over installation (fake codex/uv; injected runner).

Design 4.1 harness.py fields: name, display, wake, durable_wake, needs_session, session_hint, skill_invoke, launch_args_var, spawn_builder, spawn_prompt, install_cta, debug_hint. 4.2 Wake: build_wake_payload/build_reaction_payload pure; ChannelWaker (sync push, returns True); CodexQueueWaker (daemon thread runs codex queue --thread <session> --message <content>, one in flight, wake() returns False when busy → bookkeeping withheld, retried next tick with recomputed content; reaction cursor commits when no wake needed); make_waker returns None when needs_session and no session; re-nudge only when not durable_wake; waker re-resolved on identity or waker generation; waker_generation change also forces heartbeat + muted refresh. 4.3 Registration: harness_session/project_dir args; harness always written; harness_session stored None when omitted (manager precedent); Identity.set only when agent/team/root differ, set_harness_session always (bumps waker_generation on change); session_hint appended when needs_session and none; harness: line rendered for non-human records, (not set) when absent; project_dir unvalidated (label source only). 4.4 Adapter: adapters/codex/hooks.json (single unquoted commandWindows path), session-start.sh (captures stdin, mkdir data, exports TEAMMATE_HARNESS + UV_PROJECT_ENVIRONMENT, codex mcp add with stamp + mkdir lock, escape-aware sed extraction of session_id/cwd, EXPORTS TEAMMATE_HOOK_NOTE, re-pipes stdin to the shared hook); shared hook honours UV_PROJECT_ENVIRONMENT and _json_escape-prefixes the note on all exits; .codex-plugin/plugin.json; .agents/plugins/marketplace.json; Loki gets the Codex marketplace entry with the Claude pin. 4.5 Skill: neutral body + Harness notes; lint. 4.6 Reincarnate: build_codex_command, SPAWN_BUILDERS, harness-scoped TEAMMATE_LAUNCH_ARGS[_CODEX], which(argv[0]), prompt-carried identity; spawned_by not recorded on Codex. 4.7 Root move (v0.17.0): ~/.teammate-comms default, CLAUDE_CONFIG_DIR dropped, explicit migrate-root CLI holding a migrate lock (serialises migrations only), refuses on is_channel_alive or heartbeat < 60 s, os.replace / copy+verify, MIGRATED.json marker, doctor reports legacy state.

WPs: WP-43 harness + registration (1 d), WP-44 Codex wake (1.5 d), WP-45 packaging/hooks/skill/parity/ratchet (1.5 d), WP-46 reincarnate (0.5 d), Gate TC-C1 human field verification (docs/codex-test-brief.md), WP-47 root move (1.5 d). Real edges: 43 → {44, 46}; 45 ∥ all; gate after all four. KNOWN-INTENTIONAL and risks listed. Rulings open (§9): migration shape, drop CLAUDE_CONFIG_DIR, no-renderer skill approach, release split, harness line (not set) convention.