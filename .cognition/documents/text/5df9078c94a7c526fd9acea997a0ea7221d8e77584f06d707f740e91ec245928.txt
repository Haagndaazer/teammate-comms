WP-13 — Project profiles (v0.9.0). Manager Silvie, implementer Svetlana, branch feat/wp13-project-profiles. Goal: a first-class project-profile layer over the per-agent project field so agents discover which projects are worked and who to ask — 4 new tools (project_register, list_projects, project_profile, project_delete; 13→17 total). Locked decisions: roster DERIVED live from agent records (never stored — derive-not-store kills fragmentation), keyed by the existing project label. Data model: projects/ dir, one JSON per profile, slug via percent-encoding. §4 Key normalization = the load-bearing correctness fix: validate_project_key must normalize backslash→slash + case-fold before compare/slug, else cross-OS agents silently split the roster (acceptance criterion #2: teammate_list grouping survives a Windows-vs-Unix spelling mismatch — the criterion the fable-audit G1 finding later proved was violated by _handle_list). Storage helpers mirror agents/groups patterns; write under blocking lock with created/updated stamps (created_at==updated_at detects fresh registration — the verb bug + clock-injection test came from this). Known-intentional: open edit-by-convention, no ACLs. Acceptance suite: test_wp13_projects.py (which shipped with the C:/cctmp hardcode + 0.10.0 literal the fable-audit Q2/Q3 later fixed). Shipped @6e4f322 with gate fixes @2bdf3a3/@b91127e.