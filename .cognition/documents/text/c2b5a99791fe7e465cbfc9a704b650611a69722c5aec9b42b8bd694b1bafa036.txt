Fable Audit Stage 2 (2026-07-01), teammate-comms, seams between systems; yardstick = third-party adopters. 5 Sonnet auditors split by boundary (message lifecycle, identity/liveness, dashboard-store, plugin-harness, cross-project/OS/host). Full text: docs/260701-fable-audit-interconnectivity.md. Findings:
I1 CRITICAL bug: cross-host live name collision never resolves — is_channel_alive's pid check is host-gated so both hosts see the other alive; field-merge write_agent_record + 5s heartbeats flip record ownership forever; name-keyed inbox means both instances drain one mailbox — silent non-deterministic misdelivery (server.py:180-197, comms.py:860-883, channel.py:238-243).
G1 CRITICAL gap: teammate_list filters by raw project string, never validate_project_key — the one entry point WP-13 normalization skipped, violating WP-13's own acceptance criterion #2; cross-OS teammates silently vanish from the roster while list_projects shows them together (tools.py:815-832 vs 1578-1608, dashboard.py:257-266).
I2 high: reincarnate collision guard can't see a live human (register_human never sets channel → is_channel_alive always False; remove_teammate has the type=="human" carve-out, reincarnate forgot it) — spawned child hijacks operator identity (tools.py:1509-1529 vs 1471-1472).
I3 high bug: remove_agent unlinks with no lock and swallows OSError; teammate_delete always reports success — deletion can silently no-op on Windows (comms.py:767-778, tools.py:1479-1488).
I4 med-high: delete-then-heartbeat resurrects a type-less ghost record via the empty-dict merge path — send_dm then claims "no teammate named X registered" for a live agent (channel.py:238-243, comms.py:801-825).
G2 high: no case-folding for agent/group names — same names merge on Windows, split on Linux, on the same shared root (comms.py:31,74-84).
G3 high: default project key = parent-dir name (server.py:131-134) — same repo cloned at different paths splits rosters by default; WP-8 traded collision risk for unrecorded split risk.
G4 high: no detection of comms-root divergence between peers — "queued, teammate offline" indistinguishable from misconfiguration (comms.py:221-250, tools.py:580-617).
G5 medium: Windows reserved device names (con/aux/nul/com/lpt) pass validation, fail opaquely.
G6 medium: cross-host storage transports (SMB/NFS/OneDrive) asserted, never verified — os.replace/mkdir atomicity assumptions; sync clients violate the model.
M2 high: group fan-out "deferred" member gets zero wake signal — no retry, no marker, watcher never reads group transcripts; silently reintroduces polling (tools.py:1085-1108, channel.py:192-325).
M3 medium: watcher last_seen never seeded from durable seen.json — fresh-session wakes over-report counts (channel.py:285-296 vs tools.py:668-701).
M4 medium: react() transcript-only id resolution vs delete's 3-tier fallback — reaction wakes silently lost with TEAMMATE_TRANSCRIPT=0 (tools.py:1349-1372 vs 1386-1422).
M5 medium: ack("all") cold-start drain pollutes group read-receipts — "caught up" without having read (tools.py:770-788, comms.py:917-933).
B1 high: human presence is the flat flag the liveness design rejected — stuck online forever after non-graceful exit; cross-dashboard clobber under default name (comms.py:896-914, dashboard.py:496-514).
B3 medium: dashboard global seen-Set assumes store-wide id uniqueness — microsecond collision silently drops a message from render (index.html:189-198, comms.py:69-71).
H1 high: mid-session plugin update splits single-source-of-truth — compact hook reads new instructions from disk while server runs old code (server.py:25-26, reinject-instructions.sh:27-29).
H2 high: managed-settings allowlist detection reverse-engineered from one Claude Code build (v2.1.161), no version guard — false positive = dead silent teammate under success message.
H3 high: first-install hook-before-spawn ordering not guaranteed; documented fix is restart, prose-only, no in-product signal.
H4 medium: TEAMMATE_LAUNCH_ARGS = full allowlist bypass, never stripped, inherits down reincarnation chain forever (spawn.py:94-97, 107-138).
H5 medium: unverified foundational assumption that MCP initialize.instructions reaches the model at all.
Themes: (1) identity has no owner — name string is identity+path+collision key, merge-writes, no reconciliation (I1,I2,I4,G2,B1) — highest-leverage structural fix; (2) cross-OS/host promise fails by composition of deliberate pieces (G1,G3,G4,G6+C4+C1); (3) liveness means different things per consumer and destructive actions trust lying signals (I1-I4,B1); (4) harness contracts assumed from single observations, no guards (H1-H3,H5); (5) several paths silently reintroduce polling — invariant: every durable message must produce a wake or a visible pending indicator (M2,M3,M4,W3). 19 tasks filed under epic 291c795d20af.